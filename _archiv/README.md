# Archiv

Inhalte, die Fable in der ersten Bauphase erstellt hat, die aber **nicht auf
Lanas v1-Hauptweg** („Bewegung nach innen", 4 Stationen) liegen. Nichts ist
gelöscht — alles bleibt reaktivierbar.

## Module (`modules/`)
- **f1 — Dein zweites Nervensystem** (Gas & Bremse): Inhalt ist inzwischen in
  Modul 3 „Dein innerer Alarm" gefaltet.
- **f2 — Körper und Geist reden miteinander** (Standleitung, Zitronen-Experiment):
  Hintergrundwissen, kein eigener v1-Platz.
- **t1 — Schlaf** (scheues Reh): kandidiert als spätere Themenwelt.
- **t3 — Emotionen verdauen** (Rumi-Gästehaus): gehört Richtung Seitenpfad „Begegnen".

## Audio (`audio/`)
Die zugehörigen v2-Vertonungen (alte Stimme `v3V1d2rk6528UrLKRuy8`, Modell
`eleven_multilingual_v2`). Bei Reaktivierung ohnehin in v3 neu vertonen.

## Deko (`deko/`)
Nur von den archivierten Modulen bzw. der alten Startseite genutzte Bilder:
`wippe` (f1), `verbindung`/`zitrone` (f2), `reh` (t1), `gaestehaus` (t3),
sowie `wolken`/`drachen` (alte „Wann ist bald?"-Sektion der früheren Home).

## Reaktivieren
Datei(en) zurück nach `content/modules/` bzw. `public/audio/` + `public/deko/`
schieben, in `lib/content.ts` (`roheModule`) eintragen und in
`content/landkarte.json` einer Station zuordnen. In v3 neu vertonen.
